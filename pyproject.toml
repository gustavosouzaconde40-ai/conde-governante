[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "conde-governante"
version = "0.0.1"
description = "Conde Governante — réguas e triângulos"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["src"]
